@include "../../../stdlib/Queue"

@Queue queue

@queue.size # 0
@queue.enqueue a
@queue.enqueue b
@queue.enqueue c
@queue.size # 3
@queue.empty && echo true || echo false # false
@queue.front # a
@queue.back # c
@queue.dequeue # a
@queue.size # 2
@queue.dequeue # b
@queue.dequeue # c
@queue.size # 0
@queue.empty && echo true || echo false # true
@queue.enqueue hello
@queue.enqueue world
@queue.enqueue !
@queue.size # 3
@queue.clear
@queue.size # 0
@queue.empty && echo true || echo false # true
